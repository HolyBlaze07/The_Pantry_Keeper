import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.holyblaze07.pantrykeeper',
  appName: 'The Pantry Keeper',
  webDir: 'dist'
};

export default config;
